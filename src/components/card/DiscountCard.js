import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const DiscountCard = () => {
  return (
    <View>
      <Text>DiscountCard</Text>
    </View>
  );
};

export default DiscountCard;

const styles = StyleSheet.create({});
